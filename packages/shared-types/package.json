{
  "name": "@uwo/shared-types",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json --outDir dist",
    "lint": "echo \"No lint configured for shared-types\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}

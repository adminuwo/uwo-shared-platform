[project]
name = "uwo-shared-platform"
version = "0.1.0"
description = "Canonical shared platform services and contracts for UWO products"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
